# Deprecated

This topic has been merged into:

- `moonlift/MOONLIFT_NATIVE_COMPILER_CENTER_PLAN.md`

That document is now the single combined design plan covering:

- replacing ASDL with Moonlift `data`
- Rust-native `DataWorld`
- native memoized boundaries
- extensible language / parser extension design
